import React from 'react';

const Presentation = () => {
    return <h4>This page presents some of the experiments I've been doing, all of the data is fetched directly from online APIs or directly downloaded while loading the page. The charts were created using <a href="https://www.highcharts.com/">Highcharts</a> and the map using <a href="https://www.react-simple-maps.io/">React Simple Map</a></h4>
}

export default Presentation;